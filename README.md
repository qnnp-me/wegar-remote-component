# Wegar 远程 React 组件加载器

```tsx
<RemoteComponent
  url={'/WegarRemoteSubComponentTest.umd.cjs'}
  css
  props={{
    useStore,
    count,
    setCount
  }}>
  Loading Remote Component...
</RemoteComponent>
```